export const DEFAULT_LOCATIONS_LIST = ["A", "B", "C", "D", "E", "F", "G", "H", "I", "K"];
export const SVG_WIDTH = 200;
export const CENTER_SVG_POS = {
    x: SVG_WIDTH / 2,
    y: SVG_WIDTH / 2
}
export const CIRCLE_RADIUS = SVG_WIDTH / 2 - 20;
export const BUS_STOP_TIMEOUT = 1000 * 3;

